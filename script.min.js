function showToast(message,type = 'success'){const container = document.getElementById('toast-container');if(!container)return;const toast = document.createElement('div');toast.className = `toast ${type}`;toast.textContent = message;requestAnimationFrame(()=>{container.appendChild(toast);requestAnimationFrame(()=>{toast.style.opacity = '1';});});setTimeout(()=>{toast.style.opacity = '0';setTimeout(()=> toast.remove(),300);},3000);}const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;function isValidEmail(email){return emailRegex.test(email);}function throttle(func,limit){let inThrottle;return function(...args){if(!inThrottle){func.apply(this,args);inThrottle = true;setTimeout(()=> inThrottle = false,limit);}};}function debounce(func,wait){let timeout;return function executedFunction(...args){const later =()=>{clearTimeout(timeout);func(...args);};clearTimeout(timeout);timeout = setTimeout(later,wait);};}const createMobileMenu =()=>{const nav = document.querySelector('nav');const navLinks = document.querySelector('.nav-links');if(!nav || !navLinks)return;const menuToggle = document.createElement('button');menuToggle.className = 'menu-toggle';menuToggle.innerHTML = '<i class="fas fa-bars" aria-hidden="true"></i>';menuToggle.setAttribute('aria-label','Toggle navigation menu');menuToggle.setAttribute('aria-expanded','false');const logo = document.querySelector('.logo');if(logo){logo.after(menuToggle);}menuToggle.addEventListener('click',(e)=>{e.stopPropagation();requestAnimationFrame(()=>{const isActive = navLinks.classList.toggle('active');const icon = menuToggle.querySelector('i');if(isActive){icon.className = 'fas fa-times';menuToggle.setAttribute('aria-expanded','true');}else{icon.className = 'fas fa-bars';menuToggle.setAttribute('aria-expanded','false');}});},{passive:false});navLinks.querySelectorAll('a').forEach(link =>{link.addEventListener('click',()=>{requestAnimationFrame(()=>{navLinks.classList.remove('active');const icon = menuToggle.querySelector('i');if(icon){icon.className = 'fas fa-bars';}menuToggle.setAttribute('aria-expanded','false');});},{passive:true});});document.addEventListener('click',(e)=>{if(!nav.contains(e.target)&& navLinks.classList.contains('active')){requestAnimationFrame(()=>{navLinks.classList.remove('active');const icon = menuToggle.querySelector('i');if(icon){icon.className = 'fas fa-bars';}menuToggle.setAttribute('aria-expanded','false');});}},{passive:true});};if(document.readyState === 'loading'){document.addEventListener('DOMContentLoaded',createMobileMenu);}else{createMobileMenu();}let cachedNavHeight = 0;const updateNavHeight =()=>{const nav = document.querySelector('nav');if(nav){cachedNavHeight = nav.offsetHeight;}};window.addEventListener('load',updateNavHeight,{passive:true,once:true});window.addEventListener('resize',debounce(updateNavHeight,250),{passive:true});const initSmoothScrolling =()=>{document.querySelectorAll('a[href^="#"]').forEach(anchor =>{anchor.addEventListener('click',function(e){const href = this.getAttribute('href');if(href === '#')return;e.preventDefault();const target = document.querySelector(href);if(target){if(cachedNavHeight === 0)updateNavHeight();requestAnimationFrame(()=>{const targetPosition = target.getBoundingClientRect().top + window.pageYOffset - cachedNavHeight;window.scrollTo({top:targetPosition,behavior:'smooth'});});}},{passive:false});});};if(document.readyState === 'loading'){document.addEventListener('DOMContentLoaded',initSmoothScrolling);}else{initSmoothScrolling();}const initNonCriticalFeatures =()=>{const initScrollAnimations =()=>{const observerOptions ={threshold:0.1,rootMargin:'0px 0px -100px 0px'};const scrollObserver = new IntersectionObserver(function(entries){requestAnimationFrame(()=>{entries.forEach(entry =>{if(entry.isIntersecting){entry.target.style.opacity = '1';entry.target.style.transform = 'translateY(0)';}});});},observerOptions);try{document.querySelectorAll('section,.work-item,.service-card').forEach(el =>{el.style.opacity = '0';el.style.transform = 'translateY(30px)';el.style.transition = 'opacity 0.6s ease,transform 0.6s ease';el.style.willChange = 'opacity,transform';scrollObserver.observe(el);});}catch(error){console.error('Error setting up scroll observer:',error);}};scrollObserver.observe(el);});}catch(error){console.error('Error setting up scroll observer:',error);}const dots = document.querySelectorAll('.dot');if(dots.length > 0){let currentDot = 0;const rotateDots =()=>{requestAnimationFrame(()=>{if(dots[currentDot]){dots[currentDot].classList.remove('active');dots[currentDot].setAttribute('aria-selected','false');}currentDot =(currentDot + 1)% dots.length;if(dots[currentDot]){dots[currentDot].classList.add('active');dots[currentDot].classList('aria-selected','true');}});};setInterval(rotateDots,3000);}const newsletterForm = document.getElementById('newsletter-form');if(newsletterForm){newsletterForm.addEventListener('submit',function(e){e.preventDefault();const input = this.querySelector('input[type="email"]');const email = input.value.trim();if(!email){showToast('Please enter your email address','error');return;}if(!isValidEmail(email)){showToast('Please enter a valid email address','error');return;}showToast('Thank you for subscribing!','success');input.value = '';},{passive:false});}const skillBars = document.querySelectorAll('.skill-progress');if(skillBars.length > 0){const skillObserver = new IntersectionObserver((entries)=>{requestAnimationFrame(()=>{entries.forEach(entry =>{if(entry.isIntersecting){const progressBar = entry.target;const width = progressBar.style.width;progressBar.style.width = '0%';requestAnimationFrame(()=>{setTimeout(()=>{progressBar.style.transition = 'width 1.5s ease';progressBar.style.width = width;progressBar.style.willChange = 'width';},100);});skillObserver.unobserve(progressBar);}});});},{threshold:0.5});skillBars.forEach(bar =>{try{skillObserver.observe(bar);}catch(error){console.error('Error observing skill bar:',error);}});}let cachedVh = 0;const updateViewportHeight =()=>{const newVh = window.innerHeight * 0.01;if(Math.abs(newVh - cachedVh)> 0.01){cachedVh = newVh;requestAnimationFrame(()=>{document.documentElement.style.setProperty('--vh',`${cachedVh}px`);});}};const handleOrientationChange = debounce(updateViewportHeight,100);window.addEventListener('orientationchange',handleOrientationChange,{passive:true});window.addEventListener('resize',debounce(updateViewportHeight,250),{passive:true});updateViewportHeight();const handleScroll = throttle(()=>{const scrollY = window.pageYOffset;},100);window.addEventListener('scroll',handleScroll,{passive:true});document.addEventListener('error',function(e){if(e.target.tagName === 'IMG' && !e.target.dataset.errorHandled){e.target.dataset.errorHandled = 'true';console.warn('Image failed to load:',e.target.src);}},true);document.querySelectorAll('form').forEach(form =>{form.addEventListener('submit',function(e){const inputs = this.querySelectorAll('input[required]');let isValid = true;for(const input of inputs){if(!input.value.trim()){isValid = false;break;}}if(!isValid){e.preventDefault();}},{passive:false});});document.querySelectorAll('button[type="submit"]').forEach(button =>{button.addEventListener('click',function(){if(this.form && this.form.checkValidity()){this.disabled = true;setTimeout(()=>{this.disabled = false;},2000);}},{passive:false});});setTimeout(()=>{console.log('%c👋 Welcome to Rishi Raje Portfolio!','color:#d97236;font-size:16px;font-weight:bold;');console.log('%cInterested in collaboration? Email:rishi_raje07@yahoo.co.uk','color:#6b4423;font-size:12px;');},1000);if('performance' in window){window.addEventListener('load',()=>{const measurePerformance =()=>{const perfData = performance.getEntriesByType('navigation')[0];if(perfData){console.log(`Page load time:${Math.round(perfData.loadEventEnd - perfData.loadEventStart)}ms`);}};if('requestIdleCallback' in window){requestIdleCallback(measurePerformance);}else{setTimeout(measurePerformance,0);}},{passive:true,once:true});}if('IntersectionObserver' in window){const lazyImages = document.querySelectorAll('img[data-src]');if(lazyImages.length > 0){const imageObserver = new IntersectionObserver((entries)=>{requestAnimationFrame(()=>{entries.forEach(entry =>{if(entry.isIntersecting){const img = entry.target;img.src = img.dataset.src;img.removeAttribute('data-src');imageObserver.unobserve(img);}});});},{rootMargin:'50px'});lazyImages.forEach(img => imageObserver.observe(img));}}const initializeNonCriticalFeatures =()=>{if(typeof initScrollAnimations === 'function'){initScrollAnimations();}};if('requestIdleCallback' in window){requestIdleCallback(initializeNonCriticalFeatures,{timeout:2000});}else{setTimeout(initializeNonCriticalFeatures,1);}if('serviceWorker' in navigator){window.addEventListener('load',()=>{if('requestIdleCallback' in window){requestIdleCallback(()=>{});}},{passive:true,once:true});}window.addEventListener('beforeunload',()=>{if(typeof scrollObserver !== 'undefined' && scrollObserver){scrollObserver.disconnect();}},{passive:true,once:true});if(typeof module !== 'undefined' && module.exports){module.exports ={showToast,isValidEmail,createMobileMenu,throttle,debounce};}